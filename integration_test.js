// =====================================================================\n// FILE: integration_test.js (V2 - Synchronous)\n// GEP Integration Phase: Final System Test\n// Purpose: To execute a synchronous, end-to-end test of the master_resolver.js\n// =====================================================================\n\n// --- 1. IMPORT THE MASTER RESOLVER ---\nconst { resolveLegalQuery } = require('./master_resolver.js');\n\n// --- 2. DEFINE THE TEST SCENARIO ---\nconsole.log('---[ TEST SCENARIO: FAMILY LAW - INCAPACITY DUE TO AGE ]---');\n\nconst testFacts = {\n    'FL-01-F01': false, // Attained Marriageable Age? -> NO\n    'FL-01-F02': true,  // Free and Full Consent? -> YES\n    'FL-01-F03': true   // Mental Capacity to Consent? -> YES\n};\n\n// --- 3. EXECUTE THE QUERY SYNCHRONOUSLY ---\nconsole.log('Dispatching query to master_resolver.js...');\nconst finalConclusion = resolveLegalQuery('family', testFacts);\n\n// --- 4. DISPLAY THE FINAL RESULT ---\nconsole.log('\n---[ MASTER RESOLVER RESPONSE ]---');\nconsole.log(JSON.stringify(finalConclusion, null, 2));\n\n// --- 5. VALIDATE THE OUTCOME ---\nif (finalConclusion && finalConclusion.status === 'FAILURE: INCAPACITY') {\n    console.log('\n[✔] SUCCESS: The system correctly identified the legal incapacity as per RULE-FL-002.');\n} else {\n    console.log('\n[❌] FAILURE: The system did not return the expected conclusion.');\n}\n